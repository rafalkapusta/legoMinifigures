export const API_URL = `https://rebrickable.com/api/v3/lego`;

export const legoApiTags = {
    GET_FIGURE: ["GET_FIGURE"],
    GET_FIGURE_PARTS: ["GET_FIGURE_PARTS"]
};

export const FIG_NUMBER = 3;

export const phoneRegExp = /\(?([0-9]{3})\)?([ .-]?)([0-9]{3})\2([0-9]{3})/;

export const zipCodeRegExp = /^(\d{2})-(\d{3})$/;
